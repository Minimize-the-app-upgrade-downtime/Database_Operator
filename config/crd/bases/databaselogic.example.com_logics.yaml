
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: logics.databaselogic.example.com
spec:
  group: databaselogic.example.com
  names:
    kind: Logic
    listKind: LogicList
    plural: logics
    singular: logic
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Logic is the Schema for the logics API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: LogicSpec defines the desired state of Logic
          properties:
            appVersion:
              description: EPF application version. Default value "<empty>".
              type: string
            databaseVersion:
              description: EPF Database version. Default value "<empty>".
              type: string
            image:
              description: Uses the EPF image for the deployment. Default value "<empty>".
              type: string
            isUpdated:
              description: If IsUpdated true, new update available. Default value
                "false".
              type: boolean
            requestCount:
              description: ' Use for update the request count'
              minimum: 0
              type: integer
            sideCarImage:
              description: Uses the side car image deployment. Default value "<empty>".
              type: string
            sideCarIsUpdated:
              description: True, Sidecar new update available. Default value "false".
              type: boolean
            size:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file
                Replica count of the DatabaseLogic.'
              format: int32
              maximum: 10
              minimum: 1
              type: integer
          type: object
        status:
          description: LogicStatus defines the observed state of Logic
          properties:
            availableReplicas:
              format: int32
              type: integer
            podNames:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file'
              items:
                type: string
              type: array
          required:
          - availableReplicas
          - podNames
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
